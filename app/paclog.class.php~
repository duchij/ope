<?php

class paclog extends app {
    
    var $startTime;
    var $endTime;
    
    function __construct()
    {
        parent::__construct();
    } 
    
    public function init($data)
    {
        $this->runTest();
    }
    
    private function runTest()
    {
	 putenv("ODBCINI=/opt/datadirect71/odbc.ini");
	putenv("ODBCINST=/opt/datadirect71/odbcinst.ini");


        $medeaCon = $this->openOdbc2();
//exit;

//         $query =  "SELECT   *
//                    FROM ADMINSQL.VIEW_ZMENY_CR2
//                         WHERE DATUM_CAS_ZPRACOVANIA> '2015-07-03 11:00:00.000' order by ID_ZMENY with(nolock)";
        
        $query =  "SELECT   *
                   FROM ADMINSQL.VIEW_ZMENY_CR2
                        WHERE ID_ZMENY > 3555671 with(nolock)";
        
        $table = array();
        
        $this->startTime = microtime(true);
        $res = odbc_exec($medeaCon, $query);
        $this->endTime = microtime(true);
       // while ($table[] = odbc_fetch_array($res));
        //var_dump($table);
odbc_result_all($re, "BGCOLOR='#AAFFAA' border=3 width=30% bordercolordark='#FF0000'");

echo "freeing result";

odbc_free_result($res);

        
        $this->log->logData($table, false,"medea2 test");
        
        $totalTime = $this->endTime-$this->startTime;
        
        $this->log->logData($totalTime,false,"query to medea db");
        
        
       
    }
}

?>
